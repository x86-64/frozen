{ class=> "daemon/thread", loop = (uint_t)'1', ignore_errors = (uint_t)'1' },
{ class=> "io/tcp", addr = "0.0.0.0", port = (uint_t)'12345', machine = {
	{ class=>"daemon/thread", loop = (uint_t)'1' },
	{ class=>"data/transfer",
		source => {
			{ class=>"io/tcp_child" }
		},
		destination => {
			{ class=>"io/tcp_child" }
		}
	}
}
}
