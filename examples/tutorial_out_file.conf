{ class => "assign", request = {
	source      = (fd_t)"stdin",
	destination = (file_t){ filename = "example_out_file.dat" }
}},
{ class = "data/query", data = (env_t)"source", request = {
	action      = (action_t)"convert_to",
	destination = (env_t)"destination"
}},
{ class = "end" }
