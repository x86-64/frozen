{ class => "daemon/thread" },
{ class => "request/emitter",
	request = {
		request = {
			test   = (named_t) {
				name = "named_test",
				data = (raw_t) {
					length = (uint_t)'15',
					buffer = "Hello, world!\n"
				}
			},
			buffer = (named_t)"named_test"
		},
		machine = (machine_t){
			{ class => "debug" },
			{ class => "io/stdout" }
		}
	}
},
{ class => "daemon/kill" }
