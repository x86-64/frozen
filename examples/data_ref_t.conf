{ class => "daemon/thread" },
{ class => "request/emitter",
	request = {
		request = {
			temp = (ref_t){
				data = (raw_t){ buffer = "Hello, world! (If seen twice - everything is fine)\n" }
			}
		},
		machine = (machine_t){
			{ class = "query", data = (env_t)"temp", request = {
				action      = (action_t)"acquire"
			}},
			{ class = "query", data = (fd_t)"stdout", request = {
				action = (action_t)"write",
				buffer = (env_t)"temp"
			}},
			{ class = "query", data = (env_t)"temp", request = {
				action      = (action_t)"free"
			}},
			{ class = "query", data = (fd_t)"stdout", request = {
				action = (action_t)"write",
				buffer = (env_t)"temp"
			}},
			{ class = "end" }
		}
	}
},
{ class => "daemon/kill" }
