{ class => "daemon/thread" },
{ class => "emitter", request = {
	request = {
		action      = (action_t)"convert_to",
		source      = (file_t){ filename = "TODO"  },
		destination = (machine_t){
			{ class => "io/stdout"  }
		}
	},
	machine = (machine_t){
		{ class = "data/query", data = (env_t)"source" },
		{ class = "end" }
	}
}},
{ class = "kill" }
