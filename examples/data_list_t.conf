{ class => "daemon/thread" },
{ class => "request/emitter",
	request = {
		request = {
			item1  = (uint_t)"1",
			item2  = (uint_t)"2",
			list   = (list_t)""
		},
		machine = (machine_t){
			{ class = "data/query", data = (env_t)"list", request = {
				action = (action_t)"push",
				data   = (env_t)"item1"
			}},
			{ class = "data/query", data = (env_t)"list", request = {
				action = (action_t)"push",
				data   = (env_t)"item2"
			}},
			{ class = "convert", items = {
				list = {
					type   = (datatype_t)"raw_t",
					format = (format_t)"binary"
				}
			}},
			{ class = "debug" },
			{ class = "convert", items = {
				list = {
					type         = (datatype_t)"list_t",
					format       = (format_t)"binary",
					convert_from = (uint_t)"1"
				}
			}},
			{ class = "data/query", data = (env_t)"list", request = {
				action = (action_t)"pop",
				data   = (env_t)"item1"
			}},
			{ class = "data/query", data = (env_t)"list", request = {
				action = (action_t)"pop",
				data   = (env_t)"item2"
			}},
			{ class = "debug" },
			{ class = "end" }
		}
	}
},
{ class => "daemon/kill" }
