{ class => "daemon/thread" },
{ class => "request/emitter",
	request = {
		request = {
			temp   = (raw_t) { buffer = "Hello, world!\n" },
			buffer = (length_t) { data = (env_t)"temp" }
		},
		machine = (machine_t){
			{ class = "convert", items = {
				buffer = { type = (datatype_t)"raw_t", format = (format_t)"human" }
			}},
			{ class => "query", data = (fd_t)"stdout", request = {
				action = (action_t)"write",
				buffer = (env_t)"buffer"
			}},
			{ class = "end" }
		}
	}
},
{ class => "daemon/kill" }
