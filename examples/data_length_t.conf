{ class => "daemon/thread" },
{ class => "request/emitter",
	request = {
		request = {
			buffer = (raw_t) { buffer = "Hello, world!\n" },
			length = (length_t) { data = (env_t)"buffer" }
		},
		machine = (machine_t){
			{ class => "debug" },
			{ class => "io/stdout" }
		}
	}
},
{ class => "daemon/kill" }
