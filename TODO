- backend linear_hash
- working web
  - backend go_session
- autocollecting actions, like HK()
- hash recursive item instead of hash_next
- ipc (buff <=> hash)
- TODO + BUG hunt
- documentation for data, hash, backend
- release to github
- multi-threading
  - backend lock
  - backend thread_pool
  - backend queue
  - backend multi_request
- fix insert_sort
- data 'random_t' for file and testing\benchmarking suite
- new test suite, as benchmarks
- backend tcp\udp
- m4 exec for configs
- add checks to configure.ac (malloc, headers, using functions)
- add flags to configure.ac (--with-libconfig, --with-check)
- data 'range_t', 'multiselect_t' and matching routines
- mphf nelements_step with percents
