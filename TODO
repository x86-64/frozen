- fix broken slice_t hash_dump's
- backend tcp\udp
- libev
- try bind to nginx as module
- multi-threading
  - backend lock
  - backend thread_pool
  - backend queue
  - backend multi_request
- "magic" request_key_t data
- split_t inits
- simplify balancer using selector with random_t, counter_t, request_key_t instead of plain code
- action definitions and documentation
- delete ACTION_MOVE
- add ACTION_GETID or something
- rewrite incapsulate and list to use ACTION_GETID and ACTION_TRANSFER
- remove insert sort and write binary search instead
- hashes instead of incrementing
- new test suite, as benchmarks, deprecate check
- pcre
- data 'range_t', 'multiselect_t' and matching routines
- backend go_session for http
- backend_t transfers: use ACTION_CONVERT_TO functions of backend if avaliable
- data 'action_t' + convert array from\to string
- m4 exec for configs, m4 macro collection
- mphf nelements_step with percents + remove from mphf to another backend
